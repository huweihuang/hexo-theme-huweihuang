#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_ncp@2.0.0@ncp/bin/ncp" "$@"
  ret=$?
else 
  node  "$basedir/../../../_ncp@2.0.0@ncp/bin/ncp" "$@"
  ret=$?
fi
exit $ret
