#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_coffee-script@1.12.7@coffee-script/bin/coffee" "$@"
  ret=$?
else 
  node  "$basedir/../../../_coffee-script@1.12.7@coffee-script/bin/coffee" "$@"
  ret=$?
fi
exit $ret
